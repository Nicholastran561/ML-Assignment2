# ML-Assignment2
I am running Python 3.14.3 on my venv

To run the program for a specific problem be sure to change to that problems directory by using:
```
cd Problem-x
```

Example of acessing Problem 2:
```
cd Problem-2
```
Each problem has their own requirements.txt

You can install the requirements by using:
```
pip install -r requirements.txt
```